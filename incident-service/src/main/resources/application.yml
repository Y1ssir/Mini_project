# ============================================================
# application.yml - incident-service
# ============================================================
# Configuration locale (développement sans Docker).
# En mode Docker, les valeurs sont surchargées par les
# variables d'environnement et le Config Server.
# ============================================================

server:
  port: ${INCIDENT_SERVICE_PORT:8082}

spring:
  application:
    name: incident-service

  # --------------------------------------------------------
  # Config Server — charge la config centralisée depuis
  # config-repo/incident-service.yml au démarrage
  # --------------------------------------------------------
  config:
    import: optional:configserver:${CONFIG_SERVER_URI:http://localhost:8888}

  # --------------------------------------------------------
  # Base de données PostgreSQL
  # --------------------------------------------------------
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${INCIDENT_DB_NAME:incident_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      # ⚠️ validate : Flyway (V1__init_incident.sql) gère le schéma
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  # --------------------------------------------------------
  # OAuth2 Resource Server — Validation JWT Keycloak
  # ✅ CORRIGÉ : realm = incident-realm (était incident-management)
  # --------------------------------------------------------
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/incident-realm}

# --------------------------------------------------------
# Eureka Client
# --------------------------------------------------------
eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://admin:admin@localhost:8761/eureka/}
    fetch-registry: true
    register-with-eureka: true

# --------------------------------------------------------
# Spring Cloud Config Client — Authentification Basic Auth
# --------------------------------------------------------
spring.cloud.config:
  uri: ${CONFIG_SERVER_URI:http://localhost:8888}
  username: ${CONFIG_SERVER_USERNAME:configuser}
  password: ${CONFIG_SERVER_PASSWORD:configpassword}
  fail-fast: false

# --------------------------------------------------------
# Actuator
# --------------------------------------------------------
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

# --------------------------------------------------------
# Logging
# --------------------------------------------------------
logging:
  level:
    com.app.incident: DEBUG
    org.springframework.security: WARN
